using UnityEngine;

// Componente marcador vacío: identifica a los obstáculos del mapa sin necesitar un tag nuevo
// en el proyecto (los tags hay que registrarlos en ProjectSettings, esto evita tocar eso).
public class ObstacleMarker : MonoBehaviour
{
}
